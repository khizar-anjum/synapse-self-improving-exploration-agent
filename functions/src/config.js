export const config = {
  projectId: 'feb-488118',
  location: 'us-central1',
  model: 'gemini-2.0-flash',
  // Braintrust API key is read from BRAINTRUST_API_KEY env var
  // Set via: firebase functions:secrets:set BRAINTRUST_API_KEY
};
